name: tailscale-ssh-fedora-amd64-blacksmith

on:
  workflow_dispatch:

jobs:
  dotfiles-test:
    name: Tailscale SSH - Fedora AMD64
    uses: ./.github/workflows/reusable-tailscale-ssh-devenv.yml
    with:
      prefix_name: "fedora"
      runs-on: "blacksmith-2vcpu-ubuntu-2404"
      arch: "amd64"
      sudo-change-shell: true
    secrets: inherit
