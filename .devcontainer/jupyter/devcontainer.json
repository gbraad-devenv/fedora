{
    "name": "Fedora jupyter environment",
    "image": "ghcr.io/gbraad-devenv/fedora/dotfiles:41",

    "runArgs": [ "--cap-add=NET_ADMIN", "--cap-add=NET_RAW", "--device=/dev/net/tun", "--device=/dev/fuse"],
    "postStartCommand": "github/install.sh && pip install ipykernel",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.Theme-TomorrowKit",
                "ritwickdey.LiveServer",
                "ms-toolsai.jupyter",
                "ms-python.python"
            ]
        }
    }
}